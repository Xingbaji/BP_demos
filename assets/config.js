window.MYRIFORM_SITE_CONFIG = {
  // The first deployed version uses only official project-page MP4 files or
  // official YouTube embeds. Local copies remain outside the site repository.
  mediaMode: "official",
  mediaBaseUrl: "",
};
